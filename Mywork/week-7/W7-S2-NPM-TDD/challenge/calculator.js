// create your 4 function

cons = add = (a, b) => a + b;

cons = subtract = (a, b) => a - b;

cons = multiply = (a, b) => a * b;

cons = divide = (a, b) => a / b;

module.exports = {
  add,
  subtract,
  multiply,
  divide,
};
// export them
